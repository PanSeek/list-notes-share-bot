export enum TypeList {
    MAIN,
    PRIVATE,
    SHARED
}

export enum TypeActionNote {
    REFRESH,
    ADD,
    CHANGE,
    DELETE
}

export enum TypeActionList {
    REFRESH,
    ADD,
    CHANGE,
    SHARE,
    DELETE,
    LEAVE,
    KICK_USER
}